post:
  tags:
    - Authentication
  summary: Sign a user out
  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            type: object
            properties:
              message:
                type: string
    '400':
      $ref: '../../../responses/errors/bad-request.yml'
    '500':
      $ref: '../../../responses/errors/server-error.yml'