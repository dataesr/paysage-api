post:
  tags:
    - Authentication
  summary: Sign a user up
  description: Create a user account
  requestBody:
    description: Required user info for account creation
    content:
      application/json:
        schema:
          $ref: '../schemas/signup.payload.yml'
    required: true
  responses:
    '201':
      description: Created
      content:
        application/json:
          schema:
            $ref: '../schemas/sign.response.yml'
    '400':
      $ref: '../../../responses/errors/bad-request.yml'
    '500':
      $ref: '../../../responses/errors/server-error.yml'