description: Representation d'une structure
required:
  - id
allOf: 
  - type: object
    properties:
      id:
        type: string
        description: Unique identifier
        pattern: '^[a-zA-Z0-9]{8}$'
      rid:
        type: string
        description: Unique identifier
        pattern: '^[a-zA-Z0-9]{5}$'
  - $ref: './write-name.yml'
  - $ref: '../../../schemas/metas.yml'