description: Document
required:
  - id
allOf:
  - type: object
    properties:
      id:
        type: string
        pattern: '^[a-zA-Z0-9]{5}$'
      url:
        type: string
        format: uri
      mimetype:
        type: string
      originalName:
        type: string
      path:
        type: string 
  - $ref: '../schemas/write-document.yml'
  - $ref: '../../../schemas/metas.yml'