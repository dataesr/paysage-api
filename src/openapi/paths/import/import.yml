post:
  tags:
    - Import
  summary: Bulk import structures
  requestBody:
    content:
      multipart/form-data:
        schema:
          title: FilePayLoad
          type: object
          properties:
            files:
              type: string
              format: binary
          
  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            type: object
    '201':
      description: Created
      content:
        application/json:
          schema:
            type: object
            properties:
              message:
                type: string