name: types
in: query
description: |
  Les types d'objet à retourner, séparés par une virgule.
  Les types acceptés sont 'structures', 'categories' et 'persons'.
schema:
  type: string
  example: '?types=structures,persons'