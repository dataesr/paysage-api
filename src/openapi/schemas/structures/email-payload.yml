title: EmailPayload
description: Modèle servant à ajouter un email
type: object
properties:
  emailTypeId:
    type: string
    description: Unique email type identifier
    pattern: '^[a-zA-Z0-9]{15}$'
  email:
    type: string
    format: email