// TODO

// const cache = new Map();

// export const withCache = async (key, fetchFn) => {
//   const cached = cache.get(key);
//   if (cached) return cached.data;
//   const data = await fetchFn();
//   cache.set(key, data);
//   return data;
// };

// export const clearCache = () => {
//   cache.clear();
// }
